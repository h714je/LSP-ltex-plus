

# LSP-ltex-plus

Поддержка проверки грамматики LaTeX/Markdown для плагина LSP в Sublime Text, обеспечиваемая через [ltex-plus/ltex-ls-plus](https://github.com/ltex-plus/ltex-ls-plus).

Этот плагин является форком [LSP-ltex-ls](https://github.com/sublimelsp/LSP-ltex-ls), адаптированным для LTeX Plus.


## Установка

1. Установите [LSP](https://packagecontrol.io/packages/LSP) через Package Control.
2. Установите этот плагин.
3. Перезапустите Sublime. Сервер будет скачан автоматически (требуется подключение к интернету).

**Новое в v1.2.0:**
- Плагин автоматически определяет вашу платформу (Windows/macOS/Linux) и архитектуру (x64/ARM64)
- Скачивает подходящую версию **со встроенной Java** - отдельная установка Java не нужна!
- Для неподдерживаемых платформ использует платформо-независимую версию (требуется Java 21+)

**Поддерживаемые платформы со встроенной Java:**
- ✅ Windows x64
- ✅ macOS x64 (Intel)
- ✅ macOS ARM64 (Apple Silicon)
- ✅ Linux x64
- ✅ Linux ARM64

Примечание: В настоящее время LSP игнорирует файлы, не входящие в рабочую область. Добавьте папку в Sublime Text, чтобы включить сервер.

## Известные проблемы

- **Завершение процесса в Windows**: На Windows скрипт `ltex-ls-plus.bat` иногда может оставлять процесс Java запущенным после закрытия Sublime Text. Если вы столкнулись с этим, вы можете настроить запуск Java напрямую в настройках `command`:

  ```json
  "command": [
      "C:\\path\\to\\java.exe", 

      "-Xrs",
      "-Xms64m",
      "-Xmx2G",
      "-Xlog:disable", // ВАЖНО: Запрещаем Java писать логи в stdout

      "-Dapp.name=ltex-ls-plus",
      "-Dapp.home=${serverdir}",
      "-Dbasedir=${serverdir}",

      "-cp",
      "${serverdir}/lib/*",

      "org.bsplines.ltexls.LtexLanguageServerLauncher"
  ],
  ```


## Доступные команды

Доступ к командам через палитру команд (`Ctrl+Shift+P` / `Cmd+Shift+P`):

### LTeX: Clear Diagnostics
Очищает все грамматические и орфографические ошибки из текущего вида. Полезно, когда нужно временно скрыть диагностику для фокусировки на содержании.

### LTeX: Show Status
Показывает диалог с информацией о плагине:
- Версия сервера и статус установки
- Путь установки сервера
- Активный язык проверки
- Статус конфигурации внешних файлов

Помогает при диагностике проблем и проверке конфигурации.

### LTeX: Restart Server
Принудительно перезапускает языковой сервер LTeX. Используйте когда:
- Применяете изменения настроек, требующие перезапуска
- Восстанавливаетесь после ошибок сервера
- Обновляете после ручного редактирования файлов словарей


## Магические комментарии

Вы можете управлять настройками LTeX прямо в документе, используя магические комментарии. Это полезно для конфигурации отдельных документов без изменения глобальных настроек.

### Базовое использование

```latex
% LTeX: language=ru-RU
% LTeX: enabled=false

Ваш текст здесь...

% LTeX: enabled=true
```

### Примеры

**Изменить язык для раздела:**
```latex
% LTeX: language=ru-RU
\section{Введение}
Текст на русском языке.

% LTeX: language=en-US
\section{Introduction}
Text in English.
```

**Отключить конкретные правила:**
```latex
% LTeX: disabledRules=ru-RU:UPPERCASE_SENTENCE_START,RU_QUOTES
```

**Добавить слова в словарь:**
```latex
% LTeX: dictionary=en-US:LTeX,Markdown,reStructuredText
```

**Отключить проверку для блоков кода:**
```latex
% LTeX: enabled=false
\begin{lstlisting}
  код здесь
\end{lstlisting}
% LTeX: enabled=true
```

📖 **Полная до кументация:** [Руководство по магическим комментариям](https://ltex-plus.github.io/ltex-plus/advanced-usage.html#magic-comments)


## Справочник настроек

Все настройки LTeX доступны в `Preferences > Package Settings > LSP > Servers > LSP-ltex-plus > Settings`.

### Основные настройки

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| `ltex.language` | Язык проверки по умолчанию | `"en-US"` |
| `ltex.dictionary` | Пользовательские слова | `{}` |
| `ltex.disabledRules` | Отключенные правила | `{}` |
| `ltex.enabledRules` | Включенные правила | `{}` |
| `ltex.additionalRules.enablePickyRules` | Строгие правила (пассивный залог и т.д.) | `false` |
| `ltex.checkFrequency` | Когда проверять: `"edit"` или `"save"` | `"edit"` |
| `ltex.completionEnabled` | Автодополнение слов | `false` |

### Пример конфигурации

```json
{
  "settings": {
    "ltex.language": "ru-RU",
    "ltex.dictionary": {
      "ru-RU": ["LaTeX", "Markdown"]
    },
    "ltex.additionalRules.enablePickyRules": true,
    "ltex.checkFrequency": "save"
  }
}
```

### Настройки для конкретного проекта

Из палитры команд запустите `Project: Edit Project` и добавьте:

```js
{
   "settings": {
      "LSP": {
         "ltex-plus": {
            "settings": {
               // Поместите ваши настройки здесь
            }
         }
      }
   }
}
```

📖 **Полный список настроек:** [Документация настроек LTeX](https://ltex-plus.github.io/ltex-plus/settings.html)


## Внешние файлы словарей

Вместо хранения словарей в настройках (что требует перезапуска сервера при изменении), можно использовать внешние файлы.

### Как включить

```json
{
  "use_external_dictionary_files": true,
  "use_external_hidden_false_positives_files": true,
  "use_external_disabled_rules_files": true
}
```

### Преимущества

✅ **Не нужен перезапуск** - добавляйте слова без перезапуска сервера  
✅ **Чистые настройки** - файл `.sublime-settings` остается компактным  
✅ **Легко делиться** - добавьте файлы в Git для командной работы

### Расположение файлов

**Расположение по умолчанию:**
- Словари: `.../Packages/User/LSP-ltex-plus/dictionaries/{lang}.txt`
- Скрытые ложные срабатывания: `.../Packages/User/LSP-ltex-plus/hidden_false_positives/hiddenFalsePositives.{lang}.txt`
- Отключенные правила: `.../Packages/User/LSP-ltex-plus/disabled_rules/disabledRules.{lang}.txt`

**Пользовательское расположение:**
```json
{
  "use_external_dictionary_files": true,
  "external_dictionary_dir": "~/Dropbox/ltex/dictionaries"
}
```

**Формат:** Одна запись на строку (обычный текст)


## Решение проблем

### Сервер не запускается

**Симптом:** Нет проверки грамматики, нет ошибок.

**Решения:**
1. **С встроенной Java (v1.2.0+):** Должно работать автоматически
   - Windows x64, macOS x64/ARM64, Linux x64/ARM64
2. **Без встроенной Java:** Установите Java 21+ в PATH или задайте `JAVA_HOME`
3. Проверьте консоль (`View > Show Console`) на наличие ошибок

### Не показываются ошибки/предупреждения

**Симптом:** Файл открыт, но проверки не появляются.

**Решения:**
1. **Добавьте папку в workspace:** LSP требует файлы в рабочей области
   - `Project > Add Folder to Project...`
2. **Проверьте тип файла:** LaTeX, Markdown и т.д.
3. **Выполните `LTeX: Show Status`** для проверки конфигурации

### Не удается скачать сервер

**Симптом:** Загрузка прерывается или ошибка.

**Решения:**
1. Проверьте интернет и настройки прокси
2. Ручная загрузка с [GitHub Releases](https://github.com/ltex-plus/ltex-ls-plus/releases)
   - Распакуйте в `~/.../Package Storage/LSP-ltex-plus/ltex-ls-plus-{version}/`

### Проблемы с производительностью

**Симптом:** Sublime Text тормозит.

**Решения:**
1. Увеличьте Java heap:
   ```json
   "env": {
     "JAVA_OPTS": "-Xms128m -Xmx4G"
   }
   ```
2. Измените частоту на `"save"` вместо `"edit"`
3. Отключите строгие правила: `"ltex.additionalRules.enablePickyRules": false`

### Где найти логи сервера

- **Windows:** `%TEMP%` → файлы `ltex-ls`
- **macOS/Linux:** `/tmp/` → файлы `ltex-ls`


## Благодарности

*   Основано на [LSP-ltex-ls](https://github.com/sublimelsp/LSP-ltex-ls).
*   Работает на базе [ltex-plus](https://github.com/ltex-plus/ltex-ls-plus).
